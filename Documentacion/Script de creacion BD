CREATE DATABASE "Arkanoid"

CREATE TABLE PLAYER(
	nickname VARCHAR(15) PRIMARY KEY
);

CREATE TABLE SCORE(
	isScore SERIAL PRIMARY KEY,
	score INT NOT NULL,
	nickname VARCHAR(15) NOT NULL
);

ALTER TABLE SCORE
ADD CONSTRAINT fk_player_score
FOREIGN KEY (nickname) REFERENCES PLAYER(nickname);
